# Python
__pycache__/
*.pyc

# Build artifacts / generated outputs
out/
out_*/
output.wav
resampled.wav
voices/

# Runtime experiment data / outputs (canonical scripts now live under archive/)
experiments/

# Third-party model artifacts — not vendored; fetch with `make models`.
# Only `models/qnn/README.md` is ours to track; everything else under models/
# is either third-party weights or generated build output.
models/*
!models/qnn/
models/qnn/*
!models/qnn/README.md

# Third-party eval audio — regenerate with `make bench-data`.
tests/fixtures/fleurs_vi_0.wav
tests/fixtures/ref_mel_fleurs_vi_vn_0.npy

# QNN smoke test artifacts
bench/qnn/smoke/

# Backup files
*.bak
*.testbak

# Tooling / environment caches
.ruff_cache/
.venv/
.venv-qairt/

# Runtime logs (legacy logs/ is tracked under archive/)
logs/

# Benchmark harness outputs (generated)
bench-results/
eval_data/
eval_manifest_v1.json

# Caches of all types
*cache/

# Pi coding agent configurations
.pi/
.pi-subagents/

# Per-machine env vars (contains absolute paths)
.kavi.env

# Android app lives in the private kavi-android repo; not vendored here.
/android
